import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'] 
  // styles: can also be added instead of styleUrls  simimalr to adding 
  //template: instead of templetUrl
  // both are inline
})
export class AppComponent {
}
